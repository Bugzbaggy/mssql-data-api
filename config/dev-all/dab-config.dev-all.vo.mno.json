{
  "$schema": "https://github.com/Azure/data-api-builder/releases/latest/download/dab.draft.schema.json",
  "data-source": {
    "database-type": "mssql",
    "connection-string": "@env('CONN_DEVVO_AppDb_MNO_DEV')",
    "options": {
      "set-session-context": true
    }
  },
  "entities": {
    "voi_lookup_operator": {
      "description": "Look up the carrier for a voice destination prefix. Reference data: prefix ranges mapped to carrier name and country. Voice-side data lives on a different host from the messaging data, which is why this is a separate child config with its own connection key and its own reader role.",
      "source": {
        "object": "mno.Carrier_GetByPrefix",
        "type": "stored-procedure",
        "parameters": [
          {
            "name": "Prefix",
            "description": "Leading digits of the destination number, E.164 without '+'. Required.",
            "required": true,
            "default": ""
          }
        ]
      },
      "rest": {
        "enabled": true,
        "methods": ["get"]
      },
      "graphql": {
        "enabled": true,
        "operation": "query"
      },
      "permissions": [
        {
          "role": "appdb-data-api-voi-reader",
          "actions": ["execute"]
        }
      ]
    }
  }
}
