# secrets — never commit real connection strings / tokens
.env
*.env
!.env.example

# generated grounding + any DAB runtime artifacts
generated/
node_modules/

# transient dab validate output (may echo connection context)
dab-validate.log
*.validate.log

# ---- secrets & environment (added by scaffold) ----
.env
.env.*
!.env.example
!.env.stack.example
fleet.json
*.pfx
*.key
*.pem
secrets.json
appsettings.*.local.json

# ---- build output that can embed identifiers ----
dist/
build/
out/
node_modules/
__pycache__/
*.dacpac
*.exe
*.blob

# ---- editor / OS ----
.vs/
.vscode/
.idea/
.DS_Store
Thumbs.db

# Vendored DBA MCP server build output and deps
services/dba-mcp/node_modules/
services/dba-mcp/dist/

# Agent scratch (SDD progress notes, task reports). Never commit: this directory
# is excluded from ci/leak-scan.sh, so nothing in it is ever checked.
.superpowers/
